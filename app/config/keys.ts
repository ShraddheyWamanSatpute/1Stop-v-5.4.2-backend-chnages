// Back-compat wrapper for older imports that referenced `app/config/keys`.
// New canonical location is `app/backend/config/keys`.

export * from "../backend/config/keys"
export { default } from "../backend/config/keys"

